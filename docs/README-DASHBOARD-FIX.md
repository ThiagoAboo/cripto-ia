# Dashboard fix overlay

Extraia este ZIP na raiz do projeto e aceite sobrescrever.

Arquivos incluídos:
- `frontend/src/pages/DashboardPage.jsx`
- `frontend/public/favicon.ico`

Objetivo:
- remover o warning de `key` duplicada no `DashboardPage`
- remover o 404 de `favicon.ico` no Vite dev server
